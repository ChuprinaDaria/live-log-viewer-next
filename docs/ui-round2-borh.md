# Раунд 2 — борг

Зауваження рев'ю рівня P2 і нижче, які **не блокують мердж**. Кожне має
file:line і відтворення в рев'ю відповідного PR. Правило раунду: два круги
рев'ю на PR, далі рішення; P1 лейн править одразу, решта лягає сюди.

## З PR #2 (лейн E, каталог ролей і чесні стани), рев'ю раунд 5, SHA 1b2088ed

Жодного P1. Обмеження «крісло оркестратора ніколи не чекає на пульт» на
цій голові виконане: реальна проба HQ-спавну доходить до інжектованої межі
з нулем консольних проб.

1. **Каталог валідує не ті значення, які використає запуск.**
   `src/lib/roles/catalog.ts:255–256`; `src/lib/roles/registry.ts:13–16,:43–46,:58–60`.
   Каталог проганяє свіжий об'єкт дефолтів через `validateRoleParams` і
   викидає `params.value`, а запуск копіює сирий дефолт до перевірок. Тому
   перевірене нормалізоване значення й відрендерене можуть різнитися:
   select із `default:" allowed "` рендерить пробіли при пропущених
   параметрах і `allowed` при явно переданих; текстовий дефолт із 3000
   пробілів проходить як `ok`, а рендериться повністю. Окремо:
   `parameters:[{key:"constructor",…}]` заявлений запускним, але при
   пропущеному параметрі дає «invalid role parameter: constructor» — свій
   об'єкт має власний ключ, порожній успадковує `Object.prototype`.
   Читати передані параметри як власні властивості, не успадковані.

2. **Класифікація резолвера довіряє написанню імені.**
   `src/lib/agent/spawnEntryPoints.test.ts:220–231,:353–364`.
   `CLASSIFIED_SPREADS` приймає будь-який виклик з іменем
   `reportSpawnOverrides`, а окрема перевірка дивиться лише в
   `telegram/reportSpawn.ts` — зв'язку між ними немає. Однойменний
   помічник, оголошений у HQ і дописаний spread'ом після
   `resolveRole: localRoleResolution`, лишає всі шість тестів зеленими,
   хоча реальний запуск на такому коді вішається на консолі. Так само
   проходять `get resolveRole()`, лапкований і обчислений ключ.
   Менша правка: проєктувати в місці виклику лише `defer`/`internalGrant`
   явно, щоб зайва властивість помічника не могла підмінити резолвер.

3. **Нерозпізнані форми імпорту зникають замість відмови.**
   `src/lib/agent/spawnEntryPoints.test.ts:161–183,:250–252,:255–269,:329–345`.
   «Не можу прочитати» тут — порожній скан, а не відмова. Повз усі шість
   перевірок проходять `import(...).then(({executeSpawnRequest}) => …)`,
   `(await import(...)).executeSpawnRequest(req)`, каст навколо awaited
   import, шаблонний специфікатор, `require()`, лапковані ключі
   destructure, `import * as command` з наступним destructure,
   `POST["withDependencies"]`, `route.POST.withDependencies`, динамічний
   імпорт роута і ланцюг ре-експортів через два модулі. Новий `.js`-виклик
   ігнорується попри `allowJs:true`.
   Явно відмовляти непідтриманим формам і назвати підтриманий обсяг чесно.

4. **Перевірка тестового шва вже хибна на поточному дереві.**
   `src/lib/mcp/receiptStoreProbeChild.ts:94` динамічно імпортує роут,
   `:231` кличе `POST.withDependencies`. Це інтеграційний виконуваний файл
   для тестів, не відкритий ендпойнт — але твердження «жоден нетестовий
   файл не тягнеться до шва» без явної класифікації тест-допоміжних файлів
   хибне. Сам шов живе на емітованому production `POST` і не керується
   ні `NODE_ENV`, ні прапорцем.

### Скорочення, рекомендовані рев'ю
Прибрати іменну евристику помічника, поблажливе оброблення невідомих
вузлів, дублюючу поаркушну перевірку (уже покрита повною мапою) і повторну
нарацію інцидентів. Не вирощувати це в загальний міжпроцедурний аналізатор:
знахідки 2–3 лікуються **зменшенням** того, що чекер намагається вивести.

## Успадковане червоне, не з цього раунду
Відтворено на чистому `49bca223` і на головах обох гілок:
- `eslint` падає репозиторно (`eslint-plugin-react` × ESLint 10,
  react/display-name loader) на будь-якому файлі;
- `MobileFocusView.keyboardInset` ×4 і `.viewport` ×1
  (`max-h-[100dvh]` проти `max-h-[var(--app-height,100dvh)]`, textarea 44
  замість 160);
- `flows/engine.test.ts` «legacy relay delivery…»;
- `spawn/route.admission.test.ts` «silent root (#341)»;
- `pipelines/severedStageRetry.test.ts:328` і `:410`,
  `pipelines/engine.test.ts:118`;
- CI dependency audit: `GHSA-p293-qw3h-jr36` і `GHSA-2xp9-vwfh-vxw4`
  проти незміненого Next 16.3.2.

## Успадковане поза обсягом раунду
- `/api/flows` не автентифікує творця, `reserveReviewerSpawn` іде з
  `creatorConversationId:null` (`src/app/api/flows/route.ts:17`,
  `src/lib/flows/engine.ts:242`). Поки це так, «некласифікована роль не
  делегує нічого» вірне не для всіх входів.

## З PR #4 (лейн A, читабельність чату), рев'ю раунд 4, SHA 266917fe

Жодного P1. Рев'юер прямо фіксує: **поточний контрол змінює нуль пікселів
на всій видимій площі транскрипту** — у спокої і під справжнім
`:focus-visible`, на 2× і 3×, з допуском 0. Відкат до попереднього стилю
робить гейт червоним (219 пікселів на 2×, 475 на 3×). Усі три знахідки —
про **доказ**, відтворені інжектованими регресами, а не про те, що
відвантажується зараз.

1. **Базовий кадр «сховано» може містити ту саму фарбу.**
   `scripts/capture-round2-lane-a.ts:600,:608,:610,:648`.
   Проба ставить `visibility:hidden` лише кнопці й перевіряє лише її
   обчислену видимість. Нащадок або псевдоелемент із
   `visibility:visible` малює далі: `::before` на `top:-4px` всередині
   смужки дає `decorativeAboveBand=0`, `hiddenApplied=true`, `judge` — `[]`,
   тоді як незалежний базовий кадр із `opacity:0` показує 456/1026
   змінених пікселів під фокусом, з них 152/352 на темних гліфах.
   Гасити весь намальований піддерев'я, не лише батька.

2. **Шість пікселів — не межа фарби контрола.**
   `scripts/capture-round2-lane-a.ts:597,:599,:828–829`.
   `::before` на `top:-16px` малює в y=629..633, повністю **вище** смужки
   638..644; обидва гейти зелені. Довільні зсуви псевдоелементів не мають
   скінченного максимуму, тож будь-яке число тут — здогад. Порівнювати
   всю видиму площу транскрипту (скінченна область розміром з вікно), а
   регрес прив'язати до справжньої межі гліфів: нинішня перевірка впізнає
   контейнер `data-tts-message`, а не чорнило літер.

3. **Допуск тихо перетворює слабке перекриття на «нуль».**
   `scripts/capture-round2-lane-a.ts:557,:563,:648–649`.
   Компаратор ігнорує пікселі зі зміною каналу ≤8. Звичайна зовнішня тінь
   `rgb(0 0 0 / 0.03)` дає «нуль» при 220 реально змінених пікселях на 2×
   і 476 на 3× (12/27 на темних гліфах), максимальна дельта — рівно 8.
   Повторні знімки незміненого контрола дають нуль навіть із допуском 0,
   тобто шум тут не виправдовує поріг.

### Скорочення, рекомендовані рев'ю
Прибрати зайві знімки смуги й діагностику скрол-нудж (`bandSeen`,
`stripFollowsScroll`, `wrapperBackground`), яких `judge` не гейтить, і
замінити проксі за іменем контейнера на перевірку сфокусованої межі.
Не вирощувати це в загальний рендерер чи аналізатор CSS.

### Що підтверджено на цій голові
A1 — 1069 семплів `requestAnimationFrame`: мінімум «верх смуги мінус низ
стрічки» = 0, ніколи не від'ємний; смуга 44 при вмісті, 0→44 без драфтів.
A3 — справжня 64-символьна мітка 752,875 px і шість змішаних з RTL
догортуються нативним свайпом, кінці натисні, зазор 8 px живий, при 99+
пігулка 76,672 px. Доступність — фіолетовий inset-контур чітко видно на
всіх трьох станах лічильника. Десктоп байт-у-байт як `main`.
